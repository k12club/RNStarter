/**
 * React Native CLI config
 *
 * assets: โฟลเดอร์ที่ `npm run link-assets` (react-native-asset) จะ link เข้า iOS / Android
 * ใส่เฉพาะไฟล์ฟอนต์ (.ttf / .otf) ในโฟลเดอร์นี้ รูปภาพให้ใช้ require() ตามปกติ
 */
module.exports = {
  assets: ['./src/assets/fonts'],
};
